# 基于Python的数据爬虫与可视化云服务设计

---

## 1. 课程信息

- **课程名称**：基于Python的数据爬取系统与可视化云服务设计
- **指导教师**：张翔
- **执行学期**：20XX-20XX-X
- **学生信息**：（见附件表格）

---

## 2. 摘要

本项目设计并实现了一个基于 Python 的网页爬虫控制台系统（PyMS）。用户通过浏览器访问 Web 界面，提交目标 URL，系统自动完成网页抓取、数据清洗、去重和结果展示。项目采用前后端分离架构，后端使用 FastAPI 框架提供 RESTful API，前端使用原生 HTML/CSS/JavaScript 构建单页应用。数据存储采用 SQLite，爬虫引擎基于 requests 和 BeautifulSoup 实现。系统支持任务全生命周期管理（创建、暂停、继续、停止、删除），提供命令行式控制台和实时事件流（SSE），并支持将清洗后的数据导出为 JSON/CSV 格式。

**关键词**：Python、网页爬虫、FastAPI、数据清洗、SQLite、SSE、Web 控制台

---

## 3. 项目背景

### 3.1 Git代码版本管理的价值

Git 作为分布式版本控制系统，具有历史追踪、分支协作、代码审计和灾难恢复等核心价值。本项目使用 GitHub 托管代码仓库，通过 Git 实践了 commit、branch、merge 等核心操作，每个功能模块的实现都有对应的提交记录。

### 3.2 数据爬取与可视化的价值

数据爬取是互联网数据采集的核心技术，广泛应用于新闻聚合、市场调研、学术研究和舆情监控等领域。数据爬取必须遵循伦理规范：遵守 robots.txt 协议、控制请求频率、仅采集公开数据。数据可视化将复杂数据转化为直观图形，帮助用户快速理解数据特征。

### 3.3 项目出发点

随着互联网信息量的爆炸式增长，从海量网页中高效提取结构化数据成为重要技术需求。本项目构建一个"Web 化的爬虫控制台"——用户无需编写代码，只需通过浏览器界面提交 URL，即可完成从数据采集到清洗导出的全流程操作。

---

## 4. 项目目标

1. **掌握 Python Web 开发技术**：通过 FastAPI 框架实践 RESTful API 设计、路由管理、中间件机制等后端开发核心技能。
2. **实现完整的网页爬虫系统**：包括 URL 校验、网页抓取、HTML 解析、链接发现、深度控制等功能。
3. **构建数据清洗管道**：对原始数据进行 HTML 标签去除、文本规范化、日期格式统一、去重等处理。
4. **开发 Web 控制台前端**：使用原生 JavaScript 构建单页应用，实现任务管理、命令控制、实时事件流等功能。
5. **实践软件工程方法**：采用 Git 版本管理、模块化设计、单元测试等工程化实践。

---

## 5. 相关技术与工具

| 技术 | 说明 |
|------|------|
| Python 3.10+ | 核心编程语言，利用类型注解、dataclass 等现代特性 |
| Git | 代码版本管理，仓库托管在 GitHub |
| FastAPI | 高性能 Web 框架，支持自动文档生成、类型校验、异步处理 |
| requests | HTTP 客户端库，用于发送网页请求 |
| BeautifulSoup | HTML/XML 解析库，用于提取结构化数据 |
| SQLite | 轻量级嵌入式关系数据库，适合单机部署 |
| SSE | 服务器向客户端推送实时数据的技术 |
| wordcloud + jieba | 词云图生成库，配合中文分词实现文本可视化 |
| HTML5/CSS3/JS | 原生前端技术，不依赖前端框架 |

---

## 6. 系统设计

### 6.1 需求分析

**用例类需求：**

| 用例 | 描述 | 优先级 |
|------|------|--------|
| 创建爬取任务 | 用户输入 URL、最大页面数、爬取深度，提交创建任务 | 高 |
| 命令控制 | 通过命令行式接口控制任务（暂停/继续/停止/清洗/删除） | 高 |
| 实时事件流 | 实时展示爬取过程中的事件日志 | 中 |
| 查看结果 | 展示原始数据和清洗后的数据，支持搜索和分页 | 中 |
| 导出数据 | 将清洗结果导出为 JSON 或 CSV 文件 | 中 |

**功能边界包含：** HTTP 网页抓取、HTML 解析、任务管理、命令控制台、数据清洗去重、结果查询导出、词云图生成、SSE 事件流、SQLite 存储。

**功能边界不包含：** 浏览器渲染模式（Playwright）、分布式任务队列（Celery）、用户注册登录。

### 6.2 系统架构

```
┌─────────────────────────────────────┐
│           浏览器前端                  │
│  任务表单 │ 命令控制台 │ 结果/事件流  │
└──────────────┬──────────────────────┘
               │ HTTP / SSE
┌──────────────┴──────────────────────┐
│          FastAPI 服务                │
│  中间件(Request ID → API Key校验)    │
│  路由层(任务/命令/事件流)             │
│  服务层(任务管理/命令解析/数据清洗)    │
│  爬虫引擎(队列消费→抓取→解析→入队)   │
└──────────────┬──────────────────────┘
               │
┌──────────────┴──────────────────────┐
│        SQLite 数据库                 │
│  tasks│queue_items│event_logs        │
│  raw_items│clean_items               │
└─────────────────────────────────────┘
```

### 6.3 核心模块

| 模块 | 文件 | 职责 |
|------|------|------|
| Web 服务器 | server.py | API 路由、中间件、SSE 事件流 |
| 任务服务 | service.py | 任务 CRUD、状态迁移、队列查询 |
| 爬虫引擎 | worker.py | 网页抓取、HTML 解析、队列消费 |
| 数据清洗 | cleaning.py | 原始数据存储、清洗去重、结果导出、词云图生成 |
| 命令引擎 | command_engine.py | 命令解析、参数校验、执行分发 |
| 数据库 | db.py | SQLite 连接管理、建表、SQL 执行 |
| 安全校验 | security.py | URL 合法性校验、SSRF 防护 |
| 状态机 | state_machine.py | 任务状态枚举、迁移规则定义 |

### 6.4 数据库设计

系统使用 SQLite 数据库，包含 6 张表：

- **tasks**：任务表，存储任务基本信息、状态、进度统计
- **queue_items**：队列表，存储待爬取/已爬取/失败的 URL
- **event_logs**：事件日志表，记录任务生命周期事件
- **command_logs**：命令日志表，记录命令执行结果
- **raw_items**：原始数据表，存储抓取的原始网页数据
- **clean_items**：清洗结果表，存储规范化、去重后的数据

### 6.5 API 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /v1/crawl/submit | 创建爬取任务 |
| POST | /v1/command | 执行命令 |
| GET | /v1/tasks | 任务列表 |
| GET | /v1/tasks/{id} | 任务详情 |
| DELETE | /v1/tasks/{id} | 删除任务 |
| GET | /v1/tasks/{id}/results | 结果查询 |
| POST | /v1/tasks/{id}/export | 导出结果 |
| GET | /v1/tasks/{id}/wordcloud | 生成词云图(PNG) |
| GET | /v1/events/stream | 实时事件流(SSE) |
| GET | /v1/health | 健康检查 |

### 6.6 核心流程

**任务创建与执行流程：** 用户提交 URL → URL 校验（防 SSRF）→ 创建任务记录 → 首条 URL 入队 → 后台线程消费队列 → 抓取网页 → 解析 HTML → 提取数据 → 发现新链接入队 → 队列全部完成 → 任务结束。

**数据清洗流程：** 用户执行 clean run 命令 → 读取 raw_items → 去除 HTML 标签 → 规范化空白字符 → 统一日期格式 → 生成去重键 → 写入 clean_items。

**去重算法：** 优先使用 news_id 作为去重键；若无 news_id，则使用 title+date 的 SHA1 哈希值，利用 SQLite UNIQUE 约束实现自动去重。

**词云图生成流程：** 读取 clean_items 中清洗完成的数据 → 合并标题和正文 → jieba 中文分词 → 过滤停用词和短词 → wordcloud 生成词云图 → 返回 PNG 图片。词云图直观展示文本中的高频词汇，帮助用户快速把握文本主题。

---

## 7. 系统开发实践

### 7.1 Git 代码协作

项目采用功能驱动的提交策略，按 Day 1-11 逐步实现各功能模块。提交信息清晰描述修改内容，如 "Implement task state machine and submit endpoint"。采用 master 分支 + 功能分支的策略。

### 7.2 模块化设计

各模块职责单一：server.py 负责 HTTP 路由，service.py 负责业务逻辑，worker.py 负责爬虫引擎，cleaning.py 负责数据清洗，db.py 负责数据库操作。模块间依赖关系清晰：server → service → db，server → worker → db。

### 7.3 测试实践

项目包含 8 个测试模块共 30 个测试用例，覆盖状态机、URL 校验、任务创建、命令引擎、爬虫 Worker、数据清洗、事件流、导出功能、词云图生成等核心功能。使用 unittest 框架，采用临时目录和内存数据库隔离测试环境，使用 mock 技术模拟网络请求。

---

## 8. 系统测试与展示

### 8.1 功能测试

- **任务创建**：通过 Web 界面提交 URL，任务从 pending → running → success 正常流转
- **命令控制**：通过命令控制台执行 task status、crawl pause/resume/stop 等命令
- **数据清洗**：执行 clean run 命令，重复数据被自动去除，结果表显示清洗后数据

### 8.2 单元测试结果

全部 30 个测试用例通过，覆盖状态机、URL 校验、任务创建、命令引擎、爬虫消费、数据清洗、事件流、导出功能、词云图生成等。

### 8.3 系统界面

主界面分为：创建任务区、命令控制台、任务列表、任务详情、事件流、结果表六个区域，支持任务全生命周期管理和数据导出。

---

## 9. 总结

本项目成功实现了一个基于 Python 的网页爬虫控制台系统，主要成果包括：完整的爬虫引擎（支持 HTTP 抓取、URL 校验、HTML 解析、深度控制）、数据清洗管道（HTML 标签去除、文本规范化、去重）、词云图可视化（基于 jieba 分词和 wordcloud 生成）、Web 控制台（任务管理、命令控制、实时事件流、数据导出）以及模块化架构和工程化实践。

通过本项目，深刻体会到模块化设计的重要性、测试驱动开发的价值、Git 版本管理的必要性以及 Python 生态的优势。

---

## 10. 参考文献

1. FastAPI 官方文档. https://fastapi.tiangolo.com/
2. BeautifulSoup 官方文档. https://www.crummy.com/software/BeautifulSoup/bs4/doc/
3. requests 官方文档. https://docs.python-requests.org/
4. SQLite 官方文档. https://www.sqlite.org/docs.html
5. MDN Web Docs - Server-Sent Events. https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events
6. Python 官方文档. https://docs.python.org/3/

---

## 11. 致谢

感谢张翔老师在课程中的悉心指导，帮助我们理解了 Web 开发、数据爬取和软件工程的核心概念。感谢同学们在项目开发过程中的交流与讨论。通过这门课程的学习，不仅掌握了 Python Web 开发的实用技能，更深刻理解了工程化思维和团队协作的重要性。
